{
  "version": 2,
  "builds": [
    { "src": "client/**", "use": "@vercel/static-build" },
    { "src": "app.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "app.js" },
    { "src": "/(.*)", "dest": "client" }
  ]
}